# 01 Quickstart

Install:

```bash
pip install -e ".[dev]"
```

Run benchmark:

```bash
python -m benchmarks.runner --out benchmarks/outputs
```

Paper snapshot:

```bash
git checkout tiis-2026-inpress
```
