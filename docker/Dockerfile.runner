FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1 \
    PYTHONHASHSEED=123 \
    PIP_NO_CACHE_DIR=1

WORKDIR /workspace

COPY . .

RUN pip install --no-cache-dir ".[dev]"

CMD ["python", "-m", "dhash_repro"]
